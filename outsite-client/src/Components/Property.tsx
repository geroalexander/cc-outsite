import React, { useState } from 'react';
import { withRouter } from 'react-router-dom';

const Property = () => {
  return (
    <div className="flex justify-center self-center items-center w-full h-screen">
      <h1 className="text-7xl">Placeholder Property Page &#128522;</h1>
    </div>
  );
};

export default withRouter(Property);
